{
  "name": "scribd-downloader",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local web downloader/reader for publicly viewable Scribd documents",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.1.0",
    "playwright-core": "^1.56.0"
  }
}
